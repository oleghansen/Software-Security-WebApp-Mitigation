{% extends 'base.twig' %}

{% block content %}

    <h2>Create post</h2>

    <form action="" method="POST">
        <div id="flasherror">{{ flash.error }}</div>

        <label>Title:</label>
        <input type = "text" name="title"> <br />

        <label>Text:</label>
        <textarea cols="40" rows="10" name="content"> </textarea><br />
        <input type="text" name="str" value="{{userStr}}" hidden />
        {% if user.bankcard != null %}
        <input type="checkbox" name="showtodoctor" value="value1"> Show post to doctors<br>
        {% endif     %}
        <input type="submit" name="submit" value="Create post">
    </form>
{% endblock %}
